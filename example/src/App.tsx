import * as React from 'react';
import { NavigationBar } from './components/NavigationBar';

export const App = () => {
    return (
        <div className='App'>
            <NavigationBar />
        </div>
    );
};